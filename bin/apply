#!/bin/bash -e
source "$(dirname "$0")/../lib/init.sh"

main() {
    source "$PWD/index.sh"

    # allow modules override
    if [ "$#" -gt 0 ]; then
        modules=("$@")
    fi

    apply-modules

    success "Applied config"
}

main "$@"
